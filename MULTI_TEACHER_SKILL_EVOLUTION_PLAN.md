# 空中课堂蒸馏：多教师 Skill 蒸馏与自进化实施计划

## 1. 项目目标

把多位物理教师的网课视频，转化为可执行、可验证、可组合、可持续优化的教学能力，而不是只生成一份“老师说了什么”的摘要。

目标链路：

```text
多位教师视频
  → 图像、板书、语音和时间轴证据
  → 原子教学能力与教学转移
  → 共性能力合并、差异策略保留
  → 可执行 Teaching Skills
  → 在真实教学任务中运行和评测
  → 通过 SkillOpt 式闭环持续优化
  → 学生选择风格，系统自动选择合适策略
```

最终产品采用类似 Codex 的工作台结构：中间是教师 Agent 的对话、推理和工具调用记录；左侧是可持续更新的教学画布，可展示画图、板书、公式、实验图和课件；右侧是证据、Skill 版本、评测和进化记录。

## 2. 核心原则

1. **证据先于结论**：所有蒸馏结果必须能回到视频时间段、语音转写、关键帧、板书状态或工具调用。
2. **动态先于截图**：重点识别“老师为什么在此刻增加这条线、擦掉哪一部分、提出哪个问题”，而不只是最终板书长什么样。
3. **能力与风格分离**：正确讲解和教学策略属于能力层；语速、措辞、板书密度等属于风格层。
4. **合并共性、保留差异**：不制造一个平均化的“超级老师”。同一能力允许有多个策略变体，按学生和场景路由。
5. **自动进化必须过闸门**：系统可以自动提出 Skill 修改，但不能未经验证直接覆盖生产版本。
6. **固定测试集不可参与优化**：训练、选择和最终测试严格隔离，避免把题库答案写入 Skill。
7. **明确证据等级**：区分 `observable`、`teacher_stated`、`inferred`、`validated`，不能把推断写成事实。

## 3. 五层系统结构

### 3.1 时序证据层

输入包括视频、音频转写、关键帧、OCR、板书区域、光标/手势轨迹和时间戳。输出不只是离散帧，还要恢复连续的板书状态：

- `BoardState(t)`：某时刻画布上的对象、公式、连线、颜色与空间关系。
- `BoardDelta(t1, t2)`：新增、删除、移动、强调、重画、局部放大等变化。
- `SpeechSpan`：与画布变化对齐的语言片段。
- `EvidenceRef`：每个判断对应的原始证据位置和置信度。

### 3.2 教学转移层

以 `TeachingTransition` 作为最小蒸馏单元：

```ts
type TeachingTransition = {
  trigger: string;          // 学生可能卡在哪里，或题目出现了什么条件
  intent: string;           // 教师此刻希望学生获得什么认知变化
  speechAct: string;        // 提问、提示、解释、反例、总结等
  boardAction?: string;     // 画、擦、移动、标注、逐步显露等
  toolAction?: string;      // 几何图、函数图、仿真、检索等工具调用
  expectedLearnerChange: string;
  evidenceRefs: string[];
};
```

多个连续转移组成 `TeachingEpisode`，例如“从文字题到受力图”“用反例纠正常见误解”“先直觉后公式”。

### 3.3 多教师 Skill 库

Skill 不直接等同于某位老师，而分成三层：

- `CoreSkill`：稳定的教学目标、适用条件、约束和成功标准。
- `StrategyVariant`：同一能力的不同可执行策略，如图像化、苏格拉底提问、严谨推导、考试捷径。
- `StyleAdapter`：语言、节奏、亲和度、板书密度和举例偏好，不改变知识正确性。

核心对象：

```ts
type SkillVersion = {
  skillId: string;
  version: string;
  core: CoreSkill;
  variants: StrategyVariant[];
  styleAdapters: StyleAdapter[];
  provenance: EvidenceRef[];
  evaluation: EvaluationSummary;
  parentVersions: string[];
  status: "candidate" | "selected" | "production" | "retired";
};
```

### 3.4 教学执行层

教师 Agent 根据题目、学生画像、当前错误和已用策略选择 Skill，并调用画布、公式、函数图、几何图、仿真、课件和检索工具。每次运行保存完整轨迹：输入、选择理由、对话、画布变化、工具参数、学生反馈和结果。

### 3.5 Skill 进化层

借鉴 SkillOpt，把优化看成“冻结目标模型、优化外部 Skill 程序”：

1. 用当前 Skill 在 `D_train` 运行任务，收集成功与失败轨迹。
2. 优化器分别总结成功经验和失败原因。
3. 以有限编辑预算提出候选补丁。
4. 在 `D_selection` 上执行候选版本。
5. 只有分数严格提高且通过硬约束，才接受候选版本。
6. 把被拒绝的编辑及失败原因加入负反馈缓冲区。
7. 每轮结束做慢速元更新，比较改善、退化、持续失败和稳定成功案例。
8. `D_test` 只用于阶段性报告，永不参与编辑决策。

基础编辑操作为 `ADD / DELETE / REPLACE`；本项目扩展：

- `SPLIT`：一个 Skill 包含冲突策略时拆分。
- `MERGE`：多个证据支持同一机制时合并。
- `FORK_VARIANT`：保留同一能力下的不同教师策略。
- `RETIRE`：淘汰稳定劣化的版本，但保留历史。
- `ROUTER_UPDATE`：仅调整适用场景或学生路由，不改能力正文。

## 4. 多教师合并与分割规则

满足以下条件时优先合并：

- 触发条件、教学目标和核心机制一致；
- 执行程序兼容，没有互相冲突的前提；
- 合并后在选择集上稳定提高或至少不损失关键子群；
- 每条结论都保留教师、视频和时间段来源。

满足以下任一条件时保留变体或拆分：

- 面向不同基础、错误类型或学习阶段；
- 解释路径存在不可兼容的认知前提；
- 一种策略总体均分较高，却让某一学生子群明显退化；
- 差异主要来自有教学价值的风格，而不是噪声；
- 当前证据不足，不能证明哪一种更好。

合并算法不能只看文本相似度，应同时看触发条件、机制、工具程序、学习效果和子群表现。

## 5. 风格选择与自动路由

学生可以显式选择：

- 直觉类比型
- 严谨推导型
- 图像 / 板书型
- 启发提问型
- 考试策略型
- 自动匹配

显式风格偏好只控制呈现方式。底层 `StrategyVariant` 仍由知识正确性、学生当前错误、历史学习效果和任务阶段共同决定。系统应允许学生随时切换风格，并在相同任务上比较学习效果，而不是把“喜欢”直接等同于“学会”。

## 6. 评测与数据划分

### 硬门槛

- 知识与计算正确；
- 证据引用有效；
- 工具调用可执行且参数合法；
- 不泄漏测试答案；
- 不产生危险或误导性的教学行为。

### 质量指标

- 学习检查通过率；
- 迁移题正确率；
- 首次有效提示率与提示成本；
- 对典型错误的修复率；
- 画图/板书动作与讲解时序的一致性；
- 不同学生子群上的稳健性；
- 风格匹配度与学生主观偏好；
- 延迟、Token 和工具成本。

现有 24 条物理 benchmark 只作为冒烟基线。正式优化前扩充到至少 60–100 条，按知识点、题型、错误类型和难度分层，再固定划分为训练集、选择集和锁定测试集。

## 7. 实施阶段

### Sprint 0：冻结基线与契约

- 冻结当前 `physics-verbal-to-visual-representation` 为 `v0`。
- 固定数据划分、指标、随机种子和运行记录格式。
- 建立 `SkillVersion`、`EvidenceRef`、`EvaluationRun` 数据契约。

退出条件：同一版本可重复运行，能回看每条结果、证据和成本。

### Sprint 1：时序多模态证据

- 对齐 ASR、关键帧、OCR 与板书区域。
- 实现 `BoardState` 和 `BoardDelta`。
- 在界面中按时间轴回放“说了什么、画了什么、为什么这样判断”。

退出条件：抽样片段中的关键板书动作可追溯，人工核验通过。

### Sprint 2：原子教学转移

- 从片段抽取 `TeachingTransition`。
- 把启发、提示、画图和纠错动作编译为可执行步骤。
- 建立事实、教师陈述、推断和验证四类标签。

退出条件：蒸馏出的 Skill 能在新题上重放同类教学策略，而不是复述原视频。

### Sprint 3：多教师合并与分割

- 接入至少三位教师的同一知识单元。
- 建立候选去重、合并、拆分和来源追踪。
- 展示共性核心与教师变体之间的关系。

退出条件：不会把冲突风格强行平均，且每个变体能解释为何存在。

### Sprint 4：SkillOpt 最小闭环

- 实现 rollout、轨迹评分、候选编辑、选择集闸门和拒绝缓冲区。
- 先支持单 Skill 的 `ADD / DELETE / REPLACE`。
- 保存父版本、补丁、原因、指标变化和回滚点。

退出条件：至少一次自动候选修改在选择集上严格提升，并能完整复现。

### Sprint 5：策略路由与风格层

- 实现 `CoreSkill / StrategyVariant / StyleAdapter`。
- 支持学生选择风格和系统自动匹配。
- 增加按学生子群的效果报告。

退出条件：同一知识能力可用至少三种风格呈现，且正确性不受风格切换影响。

### Sprint 6：受控在线进化

- 收集真实教学轨迹、学习检查和人工评价。
- 候选版本在影子流量或离线重放中评估。
- 通过闸门后灰度发布，异常自动回滚。

退出条件：生产版本只能由可审计的评测结果晋升，不能由单次点赞或单个会话直接改写。

## 8. 第一条纵向切片

先围绕现有“文字题转物理图”Skill 做完整闭环：

1. 冻结当前版本和 24 条冒烟题。
2. 补充板书过程证据和可执行画图程序。
3. 建立版本库、评测运行和 SkillOpt 候选编辑。
4. 接入另外两位教师的同类讲题片段。
5. 合并稳定共性，保留启发式、严谨式和图像式变体。
6. 在新题与不同学生错误类型上验证迁移能力。

这条切片成功后，再扩展到受力分析、运动图像、电路图和实验推理。

## 9. 建议的代码交付切片

- **PR 1（当前）**：TypeScript 迁移与 Agent 教学工作台。
- **PR 2**：时序证据、板书状态与多模态回放。
- **PR 3**：Skill 版本库、数据划分和 SkillOpt 优化器 MVP。
- **PR 4**：多教师合并、分割与策略变体。
- **PR 5**：风格选择、学生路由和受控在线进化。

## 10. 暂不做的事

- 第一阶段不训练或微调基础模型，先优化外部 Skill 和工具程序。
- 未经明确授权，不模仿教师的脸、声音或可识别身份特征。
- 不允许未经选择集验证的候选 Skill 自动上线。
- 不把满意度、点赞或文本相似度作为唯一优化目标。
- 不删除失败版本和反例；它们是避免循环犯错的重要训练信号。

## 11. 论文依据与边界

本计划借鉴 Resource2Skill 的“从人类多模态资源蒸馏可执行 Skill”思路，以及 SkillOpt 的“冻结执行模型、用外部优化器和验证闸门迭代 Skill”思路。在此基础上，本项目新增多教师合并/分割、教学时序状态、学生子群路由、风格适配和受控在线进化。详细来源、原文和借鉴边界见 [`research/papers/README.md`](research/papers/README.md)。
