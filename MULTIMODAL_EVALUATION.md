# 纯文本 / 多模态蒸馏配对评测

本评测回答一个具体问题：在同一批课堂视频上，加入关键帧是否让 SkyClass 产出了更正确、更完整、更可执行的教学 Skill，同时没有引入不可接受的幻觉和成本。

## 1. 固定对照条件

对每组实验固定：

- 同一项目、同一批视频和同一份 Whisper 逐字稿。
- 同一模型、temperature、蒸馏范围（单课/共性）和 Skill 数量约束。
- 唯一变量是 `text` 或 `multimodal`。
- 两组结果使用随机编号匿名，评审者不知道产出模式。

首轮建议使用 20–30 节课，覆盖板书讲解、课件图像、实验演示、例题推导和纯口述课。不要只挑画面信息丰富的课，否则会高估多模态收益。

## 2. 评测单元

以“一条 capability 中的一条 evidence”为证据评测单元，以“一个完整 Skill”为效用评测单元。每条结论必须回看原视频、时间戳和关键帧，不用另一个大模型自动裁决真假。

## 3. 核心指标

| 指标 | 计算方式 | 回答的问题 |
| --- | --- | --- |
| 证据精确率 | 被原视频直接支持的证据数 / 全部证据数 | 有没有编造课堂事实 |
| 时间/帧可定位率 | 能在 ±5 秒内找到对应语音或画面的证据数 / 全部证据数 | 能否真正溯源 |
| 视觉增量召回 | 文本组遗漏、多模态组正确找到的视觉教学动作 / 人工标注的此类动作 | 图像是否带来新信息 |
| 视觉证据精确率 | 正确解释画面的视觉证据数 / 全部视觉证据数 | 有没有过度解读单帧 |
| Skill 可执行性 | 2 名教师独立按 1–5 分评价“何时做、做什么、观察什么、卡住怎么办” | 产物是否真能用 |
| 能力重复率 | 语义上重复的 capability 对数 / 所有 capability 对数 | 是否只是生成了更多冗余内容 |
| 耗时与费用 | 每小时视频的总耗时、请求数、图像数和 API 费用 | 收益是否值得成本 |

## 4. 人工标注规则

每条证据标注为以下一类：

1. `supported`：语音/画面直接支持结论。
2. `partially_supported`：原课有相关动作，但结论强度或教学目的被扩大。
3. `unsupported`：原课没有发生，或时间戳/帧编号不对。
4. `unverifiable`：材料不足以裁决。

单帧能证明“画面上有坐标轴”，不能单独证明“学生已经理解坐标轴”。后者需要学生回答、操作或教师检查等额外证据。

## 5. 首轮验收门槛

以下是项目的初始 gate，应在得到首批数据后重新校准：

- 多模态组证据精确率不低于纯文本组 2 个百分点以上。
- 视觉证据精确率 ≥ 90%，时间/帧可定位率 ≥ 95%。
- 在画面信息丰富课程中，视觉教学动作召回率相对纯文本至少提升 15 个百分点。
- Skill 可执行性的配对中位数不低于纯文本，且不增加重复能力。
- 记录每小时视频的额外耗时和费用；是否可接受由真实使用频率决定，不用质量指标代替成本判断。

只要证据精确率明显下降，即使生成的 capability 更多，也不应判定多模态有效。

## 6. 消融实验

当多模态有收益后，再比较：

- 仅周期抽帧。
- 周期抽帧 + 镜头切换。
- 周期抽帧 + 镜头切换 + 逐字稿视觉提示词（当前方案）。
- 关键帧上限 8 / 12 / 20，以及图像 `detail=low/high`。

消融的目标是找到最小有效配置，而不是证明“图越多越好”。

## 7. 上线后监测

每个多模态 Skill 保留 `frame_id`、时间戳、视觉观察和打包后的关键帧。持续抽样审核：

- 按视频来源、课程类型和时长分层，避免只审查成功案例。
- 单独记录 OCR/板书误读、时序误判、学生状态过度推断和错帧引用。
- 当模型、提示词或抽帧策略改变时，使用同一固定集重跑，不用不同样本的绝对分比较版本。
